#include "main.h"


/**
 * mul - multiplies two int
 * @x: the first num to multiply
 * @y: the second num to multiply
 * Return: result of multiplication of x param and y param
 */

int mul(int x, int y)
{
	return (x * y);
}
