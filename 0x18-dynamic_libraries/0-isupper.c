#include "main.h"

/**
 * _isupper - check if a letter is per
 * @x: the number to be checked
 * Return: 1 for upper letter or a 0 for else
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
